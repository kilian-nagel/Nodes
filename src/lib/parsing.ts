
/**
 * removes leading and trailing spaces from the word.
 * 
 * @param content corresponds to the string that will be trimed.
 */
export function parsePostContent(content:string){
    return content.trim();
}